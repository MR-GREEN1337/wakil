/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ai-saas-graph.s3.us-east-1.amazonaws.com', 'images.unsplash.com', 'ai-saas-graph.s3.amazonaws.com'],
      },
};

export default nextConfig;
